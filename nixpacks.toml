[phases.setup]
nixPkgs = ["python312", "ffmpeg", "libsndfile"]

[phases.install]
cmds = [
  "pip install --upgrade pip setuptools==70.3.0",
  "pip install -r backend/requirements-cloud.txt -q"
]

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT --workers 1"
