[phases.setup]
nixPkgs = ["python312", "ffmpeg"]

[phases.install]
cmds = [
  "python3 -m pip install --upgrade pip setuptools==70.3.0 -q",
  "python3 -m pip install -r backend/requirements-cloud.txt -q"
]

[start]
cmd = "python3 -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT --workers 1"
